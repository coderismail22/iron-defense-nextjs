import { AccordionDemo } from "@/components/main/AccordionDemo";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return <div><AccordionDemo/></div>;
};

export default page;
